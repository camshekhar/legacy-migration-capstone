# Copy to ~/.dbt/profiles.yml (or set DBT_PROFILES_DIR to this folder) and
# fill in values from your .env — dbt does not read .env directly.
legacy_migration_validation:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: "fhnuxen-vm43720"
      user: "SHEKHARK"
      password: "6MzENX6c2WUyhe7"
      role: "ACCOUNTADMIN"
      database: "MIGRATION_TARGET"
      warehouse: "COMPUTE_WH"
      schema: "PUBLIC"
      threads: 4
